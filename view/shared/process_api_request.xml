<?$start = microtime();?>
<Doc>
  <count><?=(($results->total_without_limits())?$results->total_without_limits() : $results->count() )?></count>
  <per_page><?=$per_page?></per_page>
  <page><?=$this_page?></page>
  <total_pages><?=$results->total_pages?></total_pages>
  <start><?=date("Y-m-d H:i:s", time())?></start>
  <results><?=trim(partial("__api", array('data'=>$results, 'api_scope'=>$api_scope, 'model_class'=>$model_class), "xml"))?></results>
  <end><?$end = microtime(); echo date("Y-m-d H:i:s", time())?></end>
  <time><?=($end-$start)?></time>
</Doc>